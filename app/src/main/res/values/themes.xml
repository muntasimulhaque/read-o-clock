<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.ReadOCLock" parent="android:Theme.Material.Light.NoActionBar">
        <!-- The field color, so the very first frame is already the app's
             world rather than a flash the Compose surface then replaces. -->
        <item name="android:windowBackground">@color/field</item>
    </style>
</resources>
